{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run SSH Connection Sound",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "outFiles": [
                "${workspaceFolder}/src/**/*.js"
            ]
        }
    ]
}